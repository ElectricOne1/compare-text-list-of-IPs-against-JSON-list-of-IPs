Inside the code, using Leafpad or Gedit , or your favorite, replace the text and Json with the full file paths and exactly what you named them, inside quotes. 
Save file , name it , using .py at the end.
in linux , remember to go to properties > permissions> check allow executing as a program. 
usage:  sudo Python3 "compare.py"  , program will spit out amounts and when it's finished , how many the new list has.  Saves a new text file with the list of IPs.
